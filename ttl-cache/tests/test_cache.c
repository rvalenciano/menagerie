/*
 * Placeholder test binary. Real assert()-based tests for cache_set /
 * cache_get / cache_delete get added test-first, one seam at a time,
 * once the TDD session starts. Nothing to assert yet — the algorithm
 * doesn't exist.
 */

int main(void)
{
    return 0;
}
