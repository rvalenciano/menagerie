[package]
name = "rate-limiter"
version = "0.1.0"
edition = "2021"

[lib]
name = "rate_limiter"
path = "src/lib.rs"

[[bin]]
name = "rate-limiter"
path = "src/main.rs"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
tower = "0.4"
